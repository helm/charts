{{- if (.Values.ignite.dataStorage.enabled) }}
kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
  name: {{ include "ignite.fullname" . }}-wal-storage-class
provisioner: {{ .Values.ignite.dataStorage.walVolume.provisioner }}
parameters:
{{ toYaml .Values.ignite.dataStorage.walVolume.provisionerParameters | indent 2 }}
{{- end }}
