{{- if (.Values.dataStorage.enabled) }}
kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
  name: {{ include "ignite.fullname" . }}-wal-storage-class
provisioner: {{ .Values.dataStorage.walVolume.provisioner }}
parameters:
{{ toYaml .Values.dataStorage.walVolume.provisionerParameters | indent 2 }}
{{- end }}
