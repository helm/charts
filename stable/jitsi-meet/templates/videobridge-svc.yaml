apiVersion: v1
kind: Service
metadata:
  name: {{ template "fullname" . }}-videobridge
  labels:
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
spec:
  ports:
  - port: 443
    protocol: TCP
    name: stream
  type: LoadBalancer
  selector:
    app: {{ template "fullname" . }}-videobridge
