# Default values for Sysdig Cloud Helm package.

image:
    repository: "sysdig/agent"
    tag: "latest"
    pullPolicy: "Always"

sysdig:
    # Required: You need your Sysdig Cloud access key before running agents.
    #AccessKey: ""

    # Optional: Key-value agent tags following the format "key:val,key2:val2".
    #AgentTags: ""
