lnd RPC can be accessed via port {{ .Values.internalServices.rpcPort }} on the following DNS name from within your cluster:
{{ template "lnd.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local

To connect to lnd RPC:

1. Forward the port for the node:

  $ kubectl port-forward --namespace {{ .Release.Namespace }} $(kubectl get pods --namespace {{ .Release.Namespace }} -l "app={{ template "lnd.name" . }},release={{ .Release.Name }}" -o jsonpath="{ .items[0].metadata.name }") {{ .Values.internalServices.rpcPort }}

2. Copy macaroon to local machine:

3. Test connection with lncli:

  $ curl --user rpcuser:rpcpassword -k http://127.0.0.1:{{ .Values.internalServices.rpcPort }} --data-binary '{"jsonrpc": "1.0", "id":"curltest", "method": "getinfo", "params": [] }' -H 'content-type: text/plain;'
