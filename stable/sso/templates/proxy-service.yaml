apiVersion: v1
kind: Service
metadata:
  name: {{ template "sso.fullname" . }}-proxy
  labels:
    app: {{ template "sso.name" . }}-proxy
    chart: {{ template "sso.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.proxy.service.type }}
  ports:
    - name: http
      port: {{ .Values.proxy.service.port }}
      targetPort: 4180
      protocol: TCP
  selector:
    app: {{ template "sso.name" . }}-proxy
    release: {{ .Release.Name }}
