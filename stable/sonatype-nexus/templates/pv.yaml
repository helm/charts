{{- if .Values.nexus.persistence.pdName -}}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.nexus.persistence.pdName }}
  labels:
{{ include "nexus.labels" . | indent 4 }}
spec:
  capacity:
    storage: {{ .Values.nexus.persistence.storageSize }}
  accessModes:
    - ReadWriteOnce
  claimRef:
    name: {{ template "nexus.fullname" . }}-data
  gcePersistentDisk:
    pdName: {{ .Values.nexus.persistence.pdName }}
    fsType: {{ .Values.nexus.persistence.fsType }}
{{- end }}
