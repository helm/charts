{{- if and .Values.hubot.scriptsRepo.enabled }}
{{- if and .Values.hubot.scriptsRepo.username .Values.hubot.scriptsRepo.password (not .Values.hubot.scriptsRepo.existingSecretName) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "hubot.fullname" . }}-scripts-repo
  labels:
{{ include "hubot.labels" . | indent 4 }}
{{- if .Values.extraLabels }}
{{ toYaml .Values.extraLabels | indent 4 }}
{{- end }}
type: Opaque
data:
  username: {{ .Values.hubot.scriptsRepo.username | b64enc | quote }}
  password: {{ .Values.hubot.scriptsRepo.password | b64enc | quote }}
{{- end }}
{{- end }}
