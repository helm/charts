daemonSet: true

prometheusExporter:
  enabled: true

env:
  AMBASSADOR_SINGLE_NAMESPACE: true
  AMBASSADOR_NO_KUBEWATCH: no_kubewatch

volumes:
  - name: nothing
    emptyDir: {}

volumeMounts:
  - mountPath: /var/nothing
    name: nothing
    readOnly: true
