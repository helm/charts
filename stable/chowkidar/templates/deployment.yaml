apiVersion: apps/v1
kind: Deployment
metadata:
{{- if .Values.chowkidar.deployment.annotations }}
  annotations:
{{ toYaml .Values.chowkidar.deployment.annotations | indent 4 }}
{{- end }}
  labels:
{{ include "chowkidar.labels.selector" . | indent 4 }}
{{ include "chowkidar.labels.chart" . | indent 4 }}
{{- if .Values.chowkidar.deployment.labels }}
{{ toYaml .Values.chowkidar.deployment.labels | indent 4 }}
{{- end }}
  name: {{ template "chowkidar.name" . }}
spec:
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
{{ include "chowkidar.labels.selector" . | indent 6 }}
  template:
    metadata:
      labels:
{{ include "chowkidar.labels.selector" . | indent 8 }}
    spec:
      containers:
      - env:
      {{- range $name, $value := .Values.chowkidar.deployment.env.open }}
      {{- if not (empty $value) }}
        - name: {{ $name | quote }}
          value: {{ $value | quote }}
      {{- end }}
      {{- end }}
      {{- $secret_name := include "chowkidar.name" . }}
      {{- range $name, $value := .Values.chowkidar.deployment.env.secret }}
      {{- if not ( empty $value) }}
        - name: {{ $name | quote }}
          valueFrom:
            secretKeyRef:
              name: {{ $secret_name }}
              key: {{ $name | quote }}
      {{- end }}
      {{- end }}
      {{- range $name, $value := .Values.chowkidar.deployment.env.field }}
      {{- if not ( empty $value) }}
        - name: {{ $name | quote }}
          valueFrom:
            fieldRef:
              fieldPath: {{ $value | quote}}
      {{- end }}
      {{- end }}
        - name: KUBERNETES_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: CONFIG_FILE_PATH
          value: {{ .Values.chowkidar.configFilePath }}
        image: "{{ .Values.chowkidar.image.name }}:{{ .Values.chowkidar.image.tag }}"
        imagePullPolicy: {{ .Values.chowkidar.image.pullPolicy }}
        name: {{ template "chowkidar.name" . }}
        volumeMounts:
        - mountPath: /configs
          name: config-volume
      serviceAccountName: {{ template "serviceAccountName" . }}
      volumes:
      - configMap:
          name: {{ template "chowkidar.name" . }}
        name: config-volume
              