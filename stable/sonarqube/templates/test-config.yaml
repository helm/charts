apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "sonarqube.fullname" . }}-tests
data:
  run.sh: |-
    @test "Testing Sonarqube UI is accessible" {
      curl --retry 12 --retry-delay 10 {{ .Release.Name }}-sonarqube:9000
    }
