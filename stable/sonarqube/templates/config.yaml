
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "sonarqube.fullname" . }}-config
data:
  {{- if .Values.sonarProperties }}
  sonar.properties:
    {{ .Values.sonarProperties | indent 4}}
  {{- end}}
