1. Get your '{{ .Values.Master.AdminUser }}' user password by running:
  printf $(kubectl get secret --namespace {{ .Release.Namespace }} {{ template "jenkins.fullname" . }} -o jsonpath="{.data.jenkins-admin-password}" | base64 --decode);echo

{{- if .Values.Master.HostName }}

2. Visit http://{{ .Values.Master.HostName }}
{{- else }}
2. Get the Jenkins URL to visit by running these commands in the same shell:
{{- if contains "NodePort" .Values.Master.ServiceType }}
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ template "jenkins.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo http://$NODE_IP:$NODE_PORT/login

{{- else if contains "LoadBalancer" .Values.Master.ServiceType }}
  NOTE: It may take a few minutes for the LoadBalancer IP to be available.
        You can watch the status of by running 'kubectl get svc --namespace {{ .Release.Namespace }} -w {{ template "jenkins.fullname" . }}'
  export SERVICE_IP=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ template "jenkins.fullname" . }} --template "{{ "{{ range (index .status.loadBalancer.ingress 0) }}{{ . }}{{ end }}" }}")
  echo http://$SERVICE_IP:{{ .Values.Master.ServicePort }}/login

{{- else if contains "ClusterIP"  .Values.Master.ServiceType }}
  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "component={{ .Release.Name }}-{{ .Values.Master.Component }}" -o jsonpath="{.items[0].metadata.name}")
  echo http://127.0.0.1:{{ .Values.Master.ServicePort }}
  kubectl --namespace {{ .Release.Namespace }} port-forward $POD_NAME {{ .Values.Master.ServicePort }}:{{ .Values.Master.ServicePort }}

{{- end }}
{{- end }}

3. Login with the password from step 1 and the username: {{ .Values.Master.AdminUser }}
{{ if .Values.Master.JCasC.enabled }}
4. Use Jenkins Configuration as Code by specifying ConfigScripts in your values.yaml file, see documentation: http://{{ .Values.Master.HostName }}/configuration-as-code and examples: https://github.com/jenkinsci/configuration-as-code-plugin/tree/master/demos
{{- end }}


{{- if or (.Values.Master.AuthorizationStrategy) (.Values.Master.SecurityRealm) (.Values.Master.NumExecutors) (.Values.Master.CSRF) (.Values.Master.CustomConfigMap) (.Values.Master.AdditionalConfig) (.Values.Master.OverwriteConfig) (.Values.Master.DisabledAgentProtocols) (.Values.Agent) (.Values.Master.CLI) }}
DEPRICATION NOTICE:
You are using the following deprecated values that will be removed shortly:
{{- if .Values.Master.AuthorizationStrategy }}
Instead of Master.AuthorizationStrategy, switch to Master.JCasC.ConfigScripts.[key-name].authorizationStrategy (https://github.com/jenkinsci/configuration-as-code-plugin/tree/master/demos/global-matrix-auth)
{{- end }}
{{- if .Values.Master.SecurityRealm }}
Instead of Master.SecurityRealm, switch to Master.JCasC.ConfigScripts.[key-name].jenkins.securityRealm (https://github.com/jenkinsci/configuration-as-code-plugin/search?q=SecurityRealm+path%3A%2Fdemos&unscoped_q=SecurityRealm+path%3A%2Fdemos)
{{- end }}
{{- if .Values.Master.NumExecutors }}
Instead of Master.NumExecutors, switch to Master.JCasC.ConfigScripts.[key-name].jenkins.numExecutors (https://github.com/jenkinsci/configuration-as-code-plugin/tree/master/demos/jenkins)
{{- end }}
{{- if .Values.Master.CSRF }}
Instead of Master.CSRF, switch to Master.JCasC.ConfigScripts.[key-name].jenkins.crumbIssuer (See https://github.com/jenkinsci/configuration-as-code-plugin/issues/408#issuecomment-409726432 also https://<jenkins-url>/configuration-as-code/reference#CrumbIssuer-standard)
{{- end }}
{{- if .Values.Master.DisabledAgentProtocols }}
Instead of Master.DisabledAgentProtocols, switch to Master.JCasC.ConfigScripts.<key_name>.jenkins.agentProtocols (https://github.com/jenkinsci/configuration-as-code-plugin/tree/master/plugin/src/test/resources/io/jenkins/plugins/casc/AgentProtocolsTest.yml)
{{- end }}
{{- if .Values.Master.CLI }}
Instead of Master.CLI, switch to Master.JCasC.ConfigScripts.<key_name>.security.remotingCLI.enabled (https://github.com/jenkinsci/configuration-as-code-plugin/tree/master/demos/jenkins/jenkins.yaml#L47)
{{- end }}
{{- if .Values.Master.CustomConfigMap }}
Instead of Master.CustomConfigMap, switch to Master.JCasC.ConfigScripts (https://github.com/jenkinsci/configuration-as-code-plugin/tree/master/demos)
{{- end }}
{{- if .Values.Master.AdditionalConfig }}
Instead of Master.AdditionalConfig, switch to Master.JCasC.ConfigScripts (https://github.com/jenkinsci/configuration-as-code-plugin/tree/master/demos)
{{- end }}
{{- if .Values.Agent }}
Instead of Agent, switch to Master.JCasC.ConfigScripts.[key-name].jenkins.clouds (https://github.com/jenkinsci/configuration-as-code-plugin/tree/master/demos/kubernetes)
{{- end }}
Please use Jenkins Configuration as Code instead: https://github.com/helm/charts/tree/master/stable/jenkins/#configuration-as-code
{{- end }}

For more information on running Jenkins on Kubernetes, visit:
https://cloud.google.com/solutions/jenkins-on-container-engine
{{- if .Values.Master.JCasC.enabled }}
For more information about Jenkins Configuration as Code, visit:
https://jenkins.io/projects/jcasc/
{{- end }}

{{- if .Values.Persistence.Enabled }}
{{- else }}
#################################################################################
######   WARNING: Persistence is disabled!!! You will lose your data when   #####
######            the Jenkins pod is terminated.                            #####
#################################################################################
{{- end }}

{{- if .Values.rbac.install }}
Configure the Kubernetes plugin in Jenkins to use the following Service Account name {{ template "jenkins.fullname" . }} using the following steps:
  Create a Jenkins credential of type Kubernetes service account with service account name {{ template "jenkins.fullname" . }}
  Under configure Jenkins -- Update the credentials config in the cloud section to use the service account credential you created in the step above.
{{- end }}
