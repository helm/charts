apiVersion: apps/v1
kind: Deployment
metadata:
{{- if .Values.konfigurator.deployment.annotations }}
  annotations:
{{ toYaml .Values.konfigurator.deployment.annotations | indent 4 }}
{{- end }}
  labels:
{{ include "konfigurator.labels.selector" . | indent 4 }}
{{ include "konfigurator.labels.chart" . | indent 4 }}
{{- if .Values.konfigurator.deployment.labels }}
{{ toYaml .Values.konfigurator.deployment.labels | indent 4 }}
{{- end }}
{{- if .Values.konfigurator.matchLabels }}
{{ toYaml .Values.konfigurator.matchLabels | indent 4 }}
{{- end }}
  name: {{ template "konfigurator.name" . }}
spec:
  replicas: 1
  selector:
    matchLabels:
{{ include "konfigurator.labels.selector" . | indent 6 }}
  template:
    metadata:
      labels:
{{ include "konfigurator.labels.selector" . | indent 8 }}
{{ include "konfigurator.labels.chart" . | indent 8 }}
{{- if .Values.konfigurator.deployment.labels }}
{{ toYaml .Values.konfigurator.deployment.labels | indent 8 }}
{{- end }}
{{- if .Values.konfigurator.matchLabels }}
{{ toYaml .Values.konfigurator.matchLabels | indent 8 }}
{{- end }}
    spec:
      containers:
        - name: {{ template "konfigurator.name" . }}
          image: "{{ .Values.konfigurator.deployment.image.name }}:{{ .Values.konfigurator.deployment.image.tag }}"
          imagePullPolicy: {{ .Values.konfigurator.deployment.image.pullPolicy }}
          ports:
          - containerPort: {{ .Values.konfigurator.deployment.containerPort }}
            name: metrics
          command:
          - Konfigurator
          env:
          {{- range $name, $value := .Values.konfigurator.deployment.env.open }}
          {{- if not (empty $value) }}
            - name: {{ $name | quote }}
              value: {{ $value | quote }}
          {{- end }}
          {{- end }}
          {{- $secret_name := include "konfigurator.fullname" . }}
          {{- range $name, $value := .Values.konfigurator.deployment.env.secret }}
          {{- if not ( empty $value) }}
            - name: {{ $name | quote }}
              valueFrom:
                secretKeyRef:
                  name: {{ $secret_name }}
                  key: {{ $name | quote }}
          {{- end }}
          {{- end }}
          {{- range $name, $value := .Values.konfigurator.deployment.env.field }}
          {{- if not ( empty $value) }}
            - name: {{ $name | quote }}
              valueFrom:
                fieldRef:
                  fieldPath: {{ $value | quote}}
          {{- end }}
          {{- end }}
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: OPERATOR_NAME
              value: "{{ template "konfigurator.name" . }}"
      serviceAccountName: {{ template "serviceAccountName" . }}
