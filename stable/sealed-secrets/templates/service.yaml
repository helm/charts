apiVersion: v1
kind: Service
metadata:
  name: {{ template "sealed-secrets.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ template "sealed-secrets.fullname" . }}
    chart: {{ template "sealed-secrets.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  ports:
    - port: 8080
  selector:
    name: {{ template "sealed-secrets.fullname" . }}
