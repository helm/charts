apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "fullname" . }}-config-cm
  labels:
    app: {{ template "fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
data:
{{ toYaml .Values.minio_server_config | indent 2 }}
