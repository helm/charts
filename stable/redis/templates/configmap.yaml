{{- if .Values.configmap }}
apiVersion: v1
kind: ConfigMap
metadata:
  labels:
{{ include "redis.labels" . | indent 4 }}
  name: {{ template "redis.fullname" . }}
data:
  redis.conf: |-
{{ .Values.configmap | indent 4 }}
{{- end }}
