apiVersion: v1
kind: Service
metadata:
  name: traefik-ingress-controller
spec:
  type: {{ .Values.serviceType }}
  selector:
    k8s-app: traefik-ingress-controller
  ports:
  - port: 80
    name: http
  - port: 443
    name: https
