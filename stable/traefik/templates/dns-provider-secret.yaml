{{- if and .Values.acme.enabled .Values.acme.dnsProvider }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "fullname" . }}-dnsprovider-config
  labels:
    app: {{ template "fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
data:
  # TODO(kevinjqiu) iterate through config variables and b64encode them.
{{- end }}
