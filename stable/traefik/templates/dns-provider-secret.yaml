{{- if and .Values.acme.enabled .Values.acme.dnsProvider.name }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "fullname" . }}-dnsprovider-config
  labels:
    app: {{ template "fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
data:
{{- range $k, $v := .Values.acme.dnsProvider.config }}
  {{ $k }}: {{ $v | b64enc }}
{{- end }}
{{- end }}
