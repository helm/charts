apiVersion: v1
kind: Service
metadata:
  name: {{ template "testdummy.fullname" . }}
  labels:
    app: {{ template "testdummy.name" . }}
    chart: {{ template "testdummy.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "testdummy.name" . }}
    release: {{ .Release.Name }}
