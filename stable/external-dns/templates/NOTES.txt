1. External Dns should now be deploying to your cluster. You can watch the status of the deployment with this command:

    kubectl get pods -w --namespace {{ .Release.Namespace }} -l "app={{ .Chart.Name }}" -l "release={{ .Release.Name }}"
