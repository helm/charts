# Service account for plugin and policy controller
apiVersion: v1
kind: ServiceAccount
metadata:
  name: genie-plugin
  namespace: {{ .Release.Namespace }}
---
{{- if .Values.includePolicy }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: genie-policy
  namespace: {{ .Release.Namespace }}
{{- end }}