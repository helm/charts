{{- if .Values.auth -}}
apiVersion: v1
kind: Secret
metadata:
  name: redis-secret
type: Opaque
data:
  {{- if .Release.IsInstall }}
  auth: {{ randAlphaNum 20 | b64enc | quote }}
  {{- end }}
{{- end -}}