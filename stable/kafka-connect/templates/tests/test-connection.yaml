apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kafka-connect.fullname" . }}-test-connection"
  labels:
{{ include "kafka-connect.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "kafka-connect.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
