apiVersion: v1
kind: Service
metadata:
  name: {{ template "redis-sentinel.fullname" . }}-master
  labels:
    app: {{ template "redis-sentinel.name" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    heritage: "{{ .Release.Service }}"
    release: "{{ .Release.Name }}"
{{- if .Values.servers.masterAnnotations }}
  annotations:
  {{- range $key, $value := .Values.servers.masterAnnotations }}
    {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
spec:
  ports:
  - port: 6379
    protocol: TCP
    targetPort: 6379
  selector:
    app: {{ template "redis-sentinel.name" . }}
    release: "{{ .Release.Name }}"
    redis-node: "true"
    redis-role: "master"
  type: "{{ .Values.servers.serviceType }}"
