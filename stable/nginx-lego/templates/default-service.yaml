apiVersion: v1
kind: Service
metadata:
  name: {{ template "fullname" . }}-default
  labels:
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: default-http-backend