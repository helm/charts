## URL of your Incoming Webhook in Slack:
slackUrl: ""

## Configuration for the deployment:
image:
  repository: willwill/kube-slack
  tag: v3.1.1
  pullPolicy: IfNotPresent
resources: {}
  # limits:
  #  cpu: 100m
  #  memory: 128Mi
  # requests:
  #  cpu: 100m
  #  memory: 128Mi

rbac:
  ## Create a ClusterRole, ClusterRoleBinding and a ServiceAccount:
  create: true
  ## This is used, if rbac.create is false:
  serviceAccountName: default
