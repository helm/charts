{{- if not .Values.slackUrl }}

WARNING:
Create an incoming webhook in slack:
In the Slack interface, add a new "Incoming WebHooks" configuration:

Set the "slackUrl" value to the Slack "Webhook URL".
{{- end }}
