apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "bitcoind.fullname" . }}
  labels:
{{ include "bitcoind.labels.standard" . | indent 4 }}
data:
{{ toYaml .Values.configurationFile | indent 2 }}
