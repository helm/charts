apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "postput.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "postput.name" . }}
    helm.sh/chart: {{ include "postput.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "postput.fullname" . }}:{{ .Values.service.port }}/healthz']
  restartPolicy: Never
