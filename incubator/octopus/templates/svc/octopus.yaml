apiVersion: v1
kind: Service
metadata:
  name: {{ template "octopus.fullname" . }}
  labels:
    app: {{ template "octopus.name" . }}
    chart: {{ template "octopus.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.octopus.service.type }}
  ports:
    - port: {{ .Values.octopus.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "octopus.name" . }}
    release: {{ .Release.Name }}
    tier: frontend
