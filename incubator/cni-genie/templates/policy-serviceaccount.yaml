{{- if .Values.ServiceAccounts.policy.create }}
{{- if .Values.includePolicy }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "cni-genie.serviceAccountName.policy" . }}
  namespace: {{ .Release.Namespace }}
{{- end }}
{{- end }}