apiVersion: v1
kind: Service
metadata:
  name: {{ template "prometheus-operator.fullname" . }}
  labels:
    k8s-app: prometheus-operator
spec:
  clusterIP: None
  ports:
  - name: http
    port: 8080
    targetPort: http
  selector:
    k8s-app: prometheus-operator
