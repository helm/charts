global:
  slack_api_url: '...'

route:
  receiver: 'default-receiver'
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 3h

receivers:
  - name: 'default-receiver'
    slack_configs:
    - channel: '@you'
      send_resolved: true
