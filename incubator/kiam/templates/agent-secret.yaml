{{- if .Values.agent.tls.useSampleData -}}
kind: Secret
apiVersion: v1
metadata:
  name: {{ .Values.agent.tls.secretName | default (printf "%s-agent-tls" (include "kiam.fullname" .)) }}
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
{{ (.Files.Glob "sample/agent-tls/*.pem").AsSecrets | indent 2 }}
{{- end -}}
