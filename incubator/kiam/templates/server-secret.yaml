{{- if .Values.server.tls.useSampleData -}}
kind: Secret
apiVersion: v1
metadata:
  name: {{ .Values.server.tls.secretName | default (printf "%s-server-tls" (include "kiam.fullname" .)) }}
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
{{ (.Files.Glob "sample/server-tls/*.pem").AsSecrets | indent 2 }}
{{- end -}}
