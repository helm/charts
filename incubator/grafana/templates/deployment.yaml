apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: {{template "fullname" .}}
  labels:
    heritage: {{.Release.Service | quote}}
    release: {{.Release.Name | quote}}
    chart: "{{.Chart.Name}}-{{.Chart.Version}}"
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: {{template "fullname" .}}
        release: {{.Release.Name | quote}}
    spec:
      containers:
      - name: grafana
        image: "{{.Values.imageName}}:{{default "latest" .Values.imageTag}}"
        imagePullPolicy: {{default "Always" .Values.pullPolicy}}
        env:
          - name: GF_SECURITY_ADMIN_USER
            value: "{{.Values.adminUser}}"
          - name: GF_SECURITY_ADMIN_PASSWORD
            value: "{{.Values.adminPassword}}"
        ports:
        - containerPort: 3000
        volumeMounts:
        - mountPath: /data
          name: storage-volume
      - name: proxy
        image: "{{.Values.proxyImageName}}:{{default "latest" .Values.proxyImageTag}}"
        imagePullPolicy: {{default "Always" .Values.pullPolicy}}
        ports:
        - containerPort: 443
        env:
        - name: TARGET_SERVICE
          value: "localhost:3000"
        volumeMounts:
        - mountPath: /etc/secrets
          name: proxy-secrets-volume
      volumes:
        - name: storage-volume
          persistentVolumeClaim:
            claimName: {{template "fullname" .}}
        - name: proxy-secrets-volume
          secret:
            secretName: {{template "proxyname" .}}
