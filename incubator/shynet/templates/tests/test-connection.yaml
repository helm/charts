apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shynet.fullname" . }}-test-connection"
  labels:
    {{- include "shynet.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shynet.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
