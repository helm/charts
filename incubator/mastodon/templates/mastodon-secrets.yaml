apiVersion: v1
data:
  db-pass: {{ .Values.database.password }}
  otp-secret: {{ .Values.otp_secret }}
  paperclip-secret: {{ .Values.paperclip_secret }}
  secret-key-base: {{ .Values.secret_key_base }}
  smtp-password: {{ .Values.smtp.password }}
kind: Secret
metadata:
  name: mastodon-{{ .Release.Name}}-secrets
type: Opaque
