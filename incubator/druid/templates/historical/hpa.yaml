{{- if .Values.historical.autoscaling.enabled }}
apiVersion: autoscaling/v2beta2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "druid.historical.fullname" . }}
  labels:
    app: {{ include "druid.name" . }}
    chart: {{ include "druid.chart" . }}
    component: "{{ .Values.name }}"
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: StatefulSet
    name: {{ include "druid.historical.fullname" . }}
  minReplicas: {{ .Values.historical.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.historical.autoscaling.maxReplicas }}
  metrics:
{{ toYaml .Values.historical.autoscaling.metrics | indent 4 }}
{{- end }}