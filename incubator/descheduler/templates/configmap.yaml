apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: {{ template "descheduler.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
  name: {{ template "descheduler.fullname" . }}
data:
  policy.yaml: |
{{ .Values.configMaps.policy_yaml | indent 4 }}
