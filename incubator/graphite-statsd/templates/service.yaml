apiVersion: v1
kind: Service
metadata:
  name: {{ template "graphite-statsd.fullname" . }}
  labels:
    app: {{ template "graphite-statsd.name" . }}
    chart: {{ template "graphite-statsd.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "graphite-statsd.name" . }}
    release: {{ .Release.Name }}
