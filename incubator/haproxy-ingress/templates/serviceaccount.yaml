{{- if or .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ if eq .Values.serviceAccount.name "" }}{{ template "haproxy-ingress.fullname" . }}{{ else }}{{ .Values.serviceAccount.name }}{{ end }}
  namespace: {{ .Release.Namespace }}
{{- end -}}