apiVersion: v1
kind: Secret
metadata:
  name: {{ include "docker-auth.name" . }}
type: Opaque
data:
  server.pem: {{ .Values.configmap.data.server.certificate | b64enc | quote }}
  server.key: {{ .Values.configmap.data.server.key | b64enc | quote }}
