{{ if .Values.networkPolicyEnable }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: forseti-server-deny-ingress
spec:
  podSelector:
    matchLabels:
      app: forseti-server
  policyTypes:
  - Ingress
{{ end }}
