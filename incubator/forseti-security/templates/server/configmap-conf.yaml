apiVersion: v1
kind: ConfigMap
metadata:
  name: forseti-server-conf
  labels:
    chart: "{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}"
    gcs_bucket: "{{ .Values.serverBucket }}"
    gcs_object: "{{ .Values.serverBucketConfigFolder }}"
data:
  forseti_conf_server.yaml: |-
    {{ .Values.serverConfigContents | b64dec | nindent 4 }}
