1. The Domoticz server can be accessed via port {{ .Values.server.httpPort }} on the following DNS name from within your cluster:

   {{ template "server.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local
