apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "kylo.name" . }}-nifi
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ template "kylo.name" . }}-nifi
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
data:
{{ (tpl (.Files.Glob "conf/nifi/*").AsConfig . ) | indent 2 }}
