apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: {{ template "prometheus-redis-exporter.fullname" . }}
  labels:
    app: {{ template "prometheus-redis-exporter.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  replicas: {{ .Values.statsd-exporter.replicaCount }}
  selector:
    matchLabels:
      app: {{ template "prometheus-redis-exporter.name" . }}
      release: {{ .Release.Name }}
  template:
    metadata:
      annotations:
{{ toYaml .Values.statsd-exporter.annotations | indent 8 }}
      labels:
        app: {{ template "prometheus-redis-exporter.name" . }}
        release: {{ .Release.Name }}
    spec:
      serviceAccountName: {{ template "prometheus-redis-exporter.serviceAccountName" . }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
          {{- range $key, $value := .Values.statsd-exporter.extraArgs }}
            - --{{ $key }}={{ $value }}
          {{- end }}
          volumeMounts:
          - mountPath: /data
            name: {{ .Values.persitentVolume.name }}
          env:
          - name: HOME
            value: /data
          ports:
            - name: metrics
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /#/status
              port: {{ .Values.service.port }}
            initialDelaySeconds: 10
            timeoutSeconds: 10
          readinessProbe:
            httpGet:
              path: /#/status
              port: {{ .Values.service.port }}
            initialDelaySeconds: 10
            timeoutSeconds: 10
          resources:
{{ toYaml .Values.statsd-exporter.resources | indent 12 }}
{{- if .Values.statsd-exporter.nodeSelector }}
      nodeSelector:
{{ toYaml .Values.statsd-exporter.nodeSelector | indent 8 }}
    {{- end }}
      volumes:
      - name: {{ .Values.persitentVolume.name }}
        persistentVolumeClaim:
          claimName: {{ .Values.persitentVolume.claimName }}prometheus-statsd-exporter