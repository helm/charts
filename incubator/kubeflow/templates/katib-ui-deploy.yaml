apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kubeflow.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "kubeflow.name" . }}
    helm.sh/chart: {{ include "kubeflow.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/component: katib-ui
spec:
  replicas: {{ .Values.katibUi.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "kubeflow.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: katib-ui
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "kubeflow.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: katib-ui
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.katibUi.image.repository }}:{{ .Values.katibUi.image.tag }}"
          imagePullPolicy: {{ .Values.katibUi.image.pullPolicy }}
          ports:
          - name: http
            containerPort: 80
            protocol: TCP
          command:
          - ./katib-ui
          # livenessProbe:
          #   httpGet:
          #     path: /
          #     port: http
          # readinessProbe:
          #   httpGet:
          #     path: /
          #     port: http
          resources:
            {{- toYaml .Values.katibUi.resources | nindent 12 }}
      {{- with .Values.katibUi.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    {{- with .Values.katibUi.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
    {{- end }}
    {{- with .Values.katibUi.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
