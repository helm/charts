apiVersion: v1
kind: Service
metadata:
  name: {{ include "ml-pipeline.fullname" . }}-api-server
  labels:
    app.kubernetes.io/name: {{ include "ml-pipeline.name" . }}
    helm.sh/chart: {{ include "ml-pipeline.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/component: api-server
spec:
  type: ClusterIP
  ports:
    - port: 8887
      targetPort: grpc
      protocol: TCP
      name: grpc
    - port: 8888
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "ml-pipeline.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: api-server
---
### FOLLOWING SERVICE IS TEMPORARY UNTIL ###
### https://github.com/kubeflow/kubeflow/issues/3287 ###
### IS RESOLVED ###

apiVersion: v1
kind: Service
metadata:
  name: ml-pipeline
  labels:
    app.kubernetes.io/name: {{ include "ml-pipeline.name" . }}
    helm.sh/chart: {{ include "ml-pipeline.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/component: api-server
spec:
  type: ClusterIP
  ports:
    - port: 8887
      targetPort: grpc
      protocol: TCP
      name: grpc
    - port: 8888
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "ml-pipeline.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: api-server