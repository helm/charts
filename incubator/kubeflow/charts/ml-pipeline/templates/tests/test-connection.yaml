apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ml-pipeline.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "ml-pipeline.name" . }}
    helm.sh/chart: {{ include "ml-pipeline.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "ml-pipeline.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
