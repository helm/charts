apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ml-pipeline.fullname" . }}-scheduled-workflow
  labels:
    app.kubernetes.io/name: {{ include "ml-pipeline.name" . }}
    helm.sh/chart: {{ include "ml-pipeline.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/component: scheduled-workflow
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "ml-pipeline.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: scheduled-workflow
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "ml-pipeline.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: scheduled-workflow
    spec:
      serviceAccountName: {{ include "ml-pipeline.fullname" . }}-scheduled-workflow
      containers:
        - name: {{ .Chart.Name }}-scheduled-workflow
          image: gcr.io/ml-pipeline/scheduledworkflow:{{ .Values.image.tag }}
          # livenessProbe:
          #   httpGet:
          #     path: /
          #     port: http
          # readinessProbe:
          #   httpGet:
          #     path: /
          #     port: http
          resources:
            {{- toYaml .Values.scheduledWorkflow.resources | nindent 12 }}
      {{- with .Values.scheduledWorkflow.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    {{- with .Values.scheduledWorkflow.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
    {{- end }}
    {{- with .Values.scheduledWorkflow.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
