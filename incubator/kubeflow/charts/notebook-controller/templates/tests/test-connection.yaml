apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "notebook-controller.fullname" . }}-test-connection"
  labels:
{{ include "notebook-controller.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "notebook-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
