apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "bitcoincashd.fullname" . }}
  labels:
{{ include "bitcoincashd.labels.standard" . | indent 4 }}
data:
{{ toYaml .Values.configurationFile | indent 2 }}
