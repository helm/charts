apiVersion: v1
kind: Service
metadata:
  name: {{ template "baget.fullname" . }}
  labels:
    app: {{ template "baget.name" . }}
    chart: {{ template "baget.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "baget.name" . }}
    release: {{ .Release.Name }}
