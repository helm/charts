apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "dashd.fullname" . }}
  labels:
{{ include "dashd.labels.standard" . | indent 4 }}
data:
{{ toYaml .Values.configurationFile | indent 2 }}
