apiVersion: v1
kind: Service
metadata:
  name: {{ template "dashd.fullname" . }}
  labels:
    app: {{ template "dashd.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  ports:
  - name: rpc
    port: {{ .Values.RPCPort }}
    targetPort: rpc
  selector:
    app: {{ template "dashd.fullname" . }}
