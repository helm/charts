{{- if .Values.ingress.enabled -}}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ template "nifi.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ template "nifi.name" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
  annotations:
    ingress.kubernetes.io/custom-request-headers: X-ProxyScheme:http||X-ProxyHost:{{ .Values.ingress.domain }}||X-ProxyPort:80
spec:
  rules:
  - host: {{ .Values.ingress.domain }}
    http:
      paths:
      - path: /
        backend:
          serviceName: {{ template "nifi.name" . }}-headless
          servicePort: {{ .Values.nifi.properties.httpPort }}
{{- end -}}
