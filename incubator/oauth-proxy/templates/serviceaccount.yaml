{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "oauth-proxy.serviceAccountName" . }}
  labels:
{{ include "oauth-proxy.labels" . | indent 4 }}
{{- end -}}
