apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "haproxy-redis.fullname" . }}-test-connection"
  labels:
{{ include "haproxy-redis.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "haproxy-redis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
