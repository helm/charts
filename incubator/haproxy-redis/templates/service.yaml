apiVersion: v1
kind: Service
metadata:
  name: {{ include "haproxy-redis.fullname" . }}
  labels:
{{ include "haproxy-redis.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "haproxy-redis.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
