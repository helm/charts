apiVersion: v1
kind: Service
metadata:
  name: spice-proxy
  namespace: kube-system
spec:
  ports:
    - port: 3128
      targetPort: spice-proxy
  externalIPs:
    - "{{ .Values.kubevirt.spiceproxy.externalip }}"
  selector:
    app: spice-proxy
