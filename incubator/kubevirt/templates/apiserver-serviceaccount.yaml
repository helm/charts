{{- if .Values.serviceAccount.apiserver.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "kubevirt.serviceAccountName.apiserver" . }}
  namespace: {{ .Values.kubevirt.namespace }}
  labels:
{{ include "labels.standard" . | indent 4 }}
{{- end -}}
